def example(param):
  print(f"hello, {param}")
# see how this is kinda like a library defining these functions and using them in exmrun.py? lets make some more!
def mode(mode):
  print(f"the machine state mode is: {mode}")
